# Built Worker bundles — generated by staging-apply.sh / production-apply.sh
dist/

# OpenTofu local state and lock files
tofu/.terraform/
tofu/.terraform.lock.hcl
tofu/terraform.tfstate
tofu/terraform.tfstate.backup
tofu/*.tfstate
tofu/*.tfstate.backup

# Populated secrets files — never commit
.env.staging
.env.production

# Bootstrap results (contain agent keys)
results/
